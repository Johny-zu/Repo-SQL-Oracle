CONN SYSTEM/<PASSWORD>;
CON HR/HR;

-- Clase de declare y ejercicios
-- Para ver impresiones
SET SERVEROUTPUT ON;


DECLARE
l_mensaje VARCHAR(100) := 'Hola mundo';
BEGIN
DBMS_OUTPUT.PUT_LINE(l_mensaje);
EXCEPTION WHEN OTHERS
THEN DBMS_OUTPUT.PUT_LINE(SQLERRM);
END;
/

DECLARE 
l_mensaje VARCHAR2(1000) := '¡Hola';
BEGIN 
DECLARE 
l_mensaje2 VARCHAR2(100) := l_mensaje || ' mundo!';
BEGIN 
DBMS_OUTPUT.PUT_LINE(l_mensaje2);
END;
EXCEPTION 
WHEN OTHERS THEN 
DBMS_OUTPUT.PUT_LINE(DBMS_UTILITY.FORMAT_ERROR_STACK);
END;
/

DECLARE
V_LAST_NAME EMPLOYEES.LAST_NAME%TYPE;
BEGIN
SELECT LAST_NAME INTO V_LAST_NAME FROM EMPLOYEES WHERE EMPLOYEE_ID = 100;
DBMS_OUTPUT.PUT_LINE(V_LAST_NAME);
END;
/

DECLARE 
V_DEPARTMEN_ID EMPLOYEES.DEPARTMENT_ID%TYPE := 10;
BEGIN
UPDATE EMPLOYEES SET SALARY = SALARY*1.20 
WHERE DEPARTMENT_ID = V_DEPARTMEN_ID;
DBMS_OUTPUT.PUT_LINE(SQL%ROWCOUNT);
END;
/

